export type UserUrl = {
    id: number,
    short_url: string,
    original_url: string,
    created_at: number
  }

export type User = {
    id: number,
    username: string,
    email: string,
    first_name: number,
    last_name: string
  }